type Query {
  job(id: ID!): Job
  company(id: ID!): Company
  jobs: [Job!]
}

type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  id: ID!
  title: String!
  company: Company!
  description: String
}
